services:
  newssearch-app:
    container_name: newssearch-app
    build:
      context: .
      dockerfile: Dockerfile
    image: newssearch:latest
    restart: unless-stopped
    ports:
      - "5001:5001"
    env_file:
      - .env
    environment:
      - TZ=Asia/Jakarta
